import { LoginForm } from "./components/LoginForm";
import * as S from "./components/styled";

export const Login = () => {
  return (
    <S.LoginContainer>
      <LoginForm />
    </S.LoginContainer>
  );
};
