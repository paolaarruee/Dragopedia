import FieldArea from "../../components/field-area";
import * as S from "./components/styled";

export const Login = () => {
  return (
    <S.LoginContainer>
      
    </S.LoginContainer>
  );
};

export default Login;
